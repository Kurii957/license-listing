language: python
python:
  - "3.9"

stages:
  - test1

jobs:
  include:
    - stage: bs4
      name: bs4 install
      script:
        - sudo pip install BeatifulSoup4
    - stage: test1
      name: test1 nazwa
      script:
        - python -m pytest test/
